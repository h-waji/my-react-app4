import styled from "styled-components";

export const Top = () => {
    return (
        <Container>
            <h2>Top page</h2>
        </Container>
    );
};

const Container = styled.div`
    text-align: center;
`;
