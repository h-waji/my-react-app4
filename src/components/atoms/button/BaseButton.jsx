import styled from "styled-components";

export const BaseButton = styled.button`
    color: white;
    padding: 8px 32px;
    border: none;
    border-radius: 16px;
    outline: nome;
    &:hover {
        cursor: pointer;
        opacity: 0.75;
    }
`;
